import classNames from 'classnames';
import './style.scss';

function PauseMenu() {
  const classes = classNames('PauseMenu');
  return (
    <div className={classes}>
      <h1>PauseMenu</h1>
    </div>
  );
}

export default PauseMenu;
